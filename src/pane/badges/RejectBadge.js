function RejectBadge(props) {
    return (
        <span style={{ backgroundColor: 'red', borderRadius: '20px', 
        padding: '5px 5px' , color: 'white', 
        border: '2px solid darkred'}}>
            Rejected
        </span>
    );
}

export default RejectBadge;